# Direct dependencies only — transitive packages (certifi, idna, urllib3,
# pluggy, packaging, ...) are resolved by pip and don't belong here.

# Ingestion
requests
python-dotenv
PyYAML

# Transformation — Spark + Delta Lake
pyspark
delta-spark
pandas
pyarrow          

# Testing
pytest
